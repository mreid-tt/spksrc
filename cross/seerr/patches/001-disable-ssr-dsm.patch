--- src/pages/_app.tsx.orig	2025-08-14 20:10:51.000000000 +0000
+++ src/pages/_app.tsx	2026-01-08 10:40:46.453325999 +0000
@@ -246,6 +246,47 @@
     vapidPublic: '',
     enablePushRegistration: false,
     locale: 'en',
+  // Disable SSR for DSM builds - SSR is incompatible with DSM's network sandboxing
+  // DSM packages cannot make HTTP requests to themselves during SSR
+  if (process.env.DISABLE_SSR === 'true') {
+    const pageProps = await App.getInitialProps(initialProps);
+    // Return complete default props to allow CSR to take over
+    return {
+      ...pageProps,
+      messages: {},
+      locale: 'en',
+      currentSettings: {
+        initialized: false,
+        applicationTitle: 'Seerr',
+        applicationUrl: '',
+        hideAvailable: false,
+        hideBlacklisted: false,
+        localLogin: true,
+        mediaServerLogin: true,
+        movie4kEnabled: false,
+        series4kEnabled: false,
+        discoverRegion: 'US',
+        streamingRegion: 'US',
+        originalLanguage: 'en',
+        mediaServerType: 2,
+        partialRequestsEnabled: false,
+        enableSpecialEpisodes: false,
+        cacheImages: false,
+        vapidPublic: '',
+        enablePushRegistration: false,
+        locale: 'en',
+        emailEnabled: false,
+        newPlexLogin: false,
+        youtubeUrl: '',
+        jellyfinHost: undefined,
+        jellyfinExternalHost: undefined,
+        jellyfinServerName: undefined,
+        jellyfinForgotPasswordUrl: undefined
+      },
+      user: undefined
+    };
+  }
+
     emailEnabled: false,
     newPlexLogin: true,
     youtubeUrl: '',
