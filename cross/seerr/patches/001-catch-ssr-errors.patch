--- src/pages/_app.tsx.orig
+++ src/pages/_app.tsx
@@ -253,6 +253,8 @@
   };
 
   if (ctx.res) {
+    try {
+      // Try original SSR logic
     // Check if app is initialized and redirect if necessary
     const response = await axios.get<PublicSettingsResponse>(
       `http://${process.env.HOST || 'localhost'}:${
@@ -296,6 +298,11 @@
         }
       }
     }
+    } catch (error) {
+      // If SSR fails (e.g., in DSM sandbox), continue without it
+      // The app will initialize client-side
+      console.error('SSR failed, continuing with client-side init:', error.message);
+    }
   }
 
   // Run the default getInitialProps for the main nextjs initialProps
