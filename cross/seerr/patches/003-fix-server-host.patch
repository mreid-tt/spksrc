--- server/index.ts.orig
+++ server/index.ts
@@ -20,7 +20,9 @@
 const handle = app.getRequestHandler();
 
 checkEnv();
-const host = process.env.HOST ?? '0.0.0.0';
+// DSM Fix: Ensure server listens on the same address SSR uses
+// If HOST is set to 127.0.0.1, server must listen on that
+const host = process.env.HOST ?? '127.0.0.1';
 const port = parseInt(process.env.PORT ?? '5055', 10);
 
 if (host) {
